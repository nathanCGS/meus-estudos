# programa que verifica a media entre dois numeros.
n1 = float(input('Digite a primeira nota: '))
n2 = float(input('Digite a segunda nota: '))
print('a media entre {:.1f} e {:.1f} é: {:.1f}'.format(n1, n2, (n1 + n2) / 2))